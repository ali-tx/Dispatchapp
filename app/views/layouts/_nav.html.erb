<nav class="navbar navbar-inverse " >
  <div class="container-fluid">
    <div class="navbar-header">
      <ul class="nav navbar-nav">
        <a class="navbar-brand" href=#>Dispatch</a>

        <% if user_signed_in? %>
          <% if current_user.admin == true  %>

        <li class="active"><%= link_to 'Home', jobs_path%></li>
        <li class="active"><%= link_to 'Driver', users_path%></li>
        <%else%>
          <li class="active"><%= link_to 'Profile', user_path(current_user)%></li>

             <%end %>
            <%end %>
      </ul>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li><a href="../users/edit"><span class="glyphicon glyphicon-user" > <%= current_user.present? ?  current_user.email : ''%></span></a></li>
      <%if current_user.present? %>
        <li><%= link_to "Logout",  destroy_user_session_path,method: :delete %></li>
      <%else   %>
        <li><%= link_to 'Login', user_session_path%></li>
      <%end %>
    </ul>
  </div>
</nav>
